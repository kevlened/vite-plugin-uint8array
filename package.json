{
  "name": "vite-plugin-uint8array",
  "version": "0.0.1-pre.0",
  "description": "Minimalist Vite plugin to import files as Uint8Arrays",
  "repository": "git@github.com:kevlened/vite-plugin-uint8array.git",
  "license": "ISC",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsup && cp root.d.ts dist/",
    "postbuild": "echo '/// <reference path=\"./root.d.ts\" />' >> dist/index.d.ts",
    "test": "npm run build && cd test && vitest run",
    "release": "npm test && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm login && npm publish"
  },
  "devDependencies": {
    "@types/node": "^20.11.23",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vitest": "^1.3.1"
  },
  "tsup": {
    "dts": true,
    "bundle": false,
    "treeshake": true,
    "target": "node18",
    "format": [
      "esm",
      "cjs"
    ],
    "entry": [
      "src/**/*.ts"
    ]
  },
  "files": [
    "dist",
    "readme.md"
  ]
}
